# Proxy URL — leave empty for direct connection
PROXY_URL=

# Number of parallel MetaAI clients
POOL_SIZE=15

# Max time a request waits in queue (ms)
QUEUE_TIMEOUT=120000

# Server port
PORT=8000

# Chrome path (not needed for Python version — uses curl_cffi instead)
# CHROME_PATH=/usr/bin/google-chrome

# Test script settings
CONCURRENCY=100
MAX_IN_FLIGHT=50
TEST_PROMPT=What are the latest developments in AI?
API_KEY=sk_live_test
